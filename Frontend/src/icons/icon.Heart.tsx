import styles from "./icon.module.css";

export const HeartIcon = () => {
  return <i className={`${styles.icon} bi bi-heart`}></i>;
};

export const HeartFillIcon = () => {
  return <i className={`${styles.icon} bi bi-heart-fill`}></i>;
};
